pluginManagement {
    repositories {
        gradlePluginPortal() // Required for retrieving the com.gradle.plugin-publish plugin
        mavenCentral()       // Additional repository for Maven artifacts
    }
}

rootProject.name = "javafx-plugin"